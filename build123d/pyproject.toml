[project]
name = "3d-models-build123d"
version = "0.1.0"
description = "Parametric 3D models built with build123d"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "build123d>=0.10.0",
]
